<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Share"
        android:id="@+id/action_edit_note_share"
        android:icon="@drawable/ic_share"
        />

    <item android:title="Note Colour"
        app:showAsAction="always"
        android:id="@+id/action_edit_note_color"
        android:icon="@drawable/ic_color"
        />

    <item android:title="Edit Labels"
        app:showAsAction="always"
        android:id="@+id/action_edit_note_edit_label"
        android:icon="@drawable/ic_label"
        />

    <item android:title="Attach"
        app:showAsAction="always"
        android:id="@+id/action_edit_note_attach"
        android:icon="@drawable/ic_attach"
        />

    <item android:title="Change text size"
        android:id="@+id/action_edit_note_text_size_change"
        android:icon="@drawable/ic_text_size"
        />

    <item android:title="Duplicate"
        android:id="@+id/action_edit_note_duplicate"
        android:icon="@drawable/ic_duplicate"
        />

    <item android:title="Delete"
        android:id="@+id/action_edit_note_delete"
        android:icon="@drawable/ic_delete"
        />



</menu>